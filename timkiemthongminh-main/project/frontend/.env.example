# Copy file này thành .env (local) — Vercel thì set trong Project Settings > Environment Variables.

# URL của backend FastAPI (Render). Local mặc định là http://localhost:8000
# (xem fallback trong src/services/apiClient.ts) nên biến này không bắt
# buộc phải set khi chạy local.
VITE_API_BASE_URL=https://ten-backend-cua-ban.onrender.com
